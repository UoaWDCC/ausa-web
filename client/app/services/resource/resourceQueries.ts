// import { useQuery } from "@tanstack/react-query"
// import ResourceService from "./resourceService"

/*
  export function useFinancialResources() {
  return useQuery({
    queryKey: ["resources", "financial"],
    queryFn: ResourceService.getFinancialResources,
  })
}

export function useAcademicResources() {
  return useQuery({
    queryKey: ["resources", "academic"],
    queryFn: ResourceService.getAcademicResources,
  })
}

export function useWellbeingResources() {
  return useQuery({
    queryKey: ["resources", "wellbeing"],
    queryFn: ResourceService.getWellbeingResources,
  })
}

export function useExternalResources() {
  return useQuery({
    queryKey: ["resources", "external"],
    queryFn: ResourceService.getExternalResources,
  })
}
*/
